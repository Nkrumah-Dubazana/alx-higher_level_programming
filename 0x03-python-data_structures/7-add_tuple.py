#!/usr/bin/Python3
def add_tuple(tuple_a(), tuple_b=()):
    result_tuple = ()
    a = (tuple_a + (0, 0))[:2]
    b = (tuple_b + (0, 0)[:2]

    result_tuple = (a[0] + b[0], a[1] + b[1]
    return result_tuple
